export function Navbar() {
  return (
    <nav className="fixed top-0 z-50 w-full h-16 bg-bg/80 backdrop-blur-md border-b border-border/50">
      {/* Navigation — logo, links, cart icon, language toggle */}
    </nav>
  )
}
