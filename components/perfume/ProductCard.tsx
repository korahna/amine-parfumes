export function ProductCard() {
  return (
    <div className="group relative overflow-hidden cursor-pointer">
      {/* Product card — image, brand, name, scent pills, price, CTA */}
    </div>
  )
}
