'use client'

export function AdminSidebar() {
  return (
    <aside className="w-60 bg-surface border-e border-border min-h-screen">
      {/* Admin sidebar — Dashboard, Produits, Commandes, Catégories, Paramètres */}
    </aside>
  )
}
